import React from 'react';

const Home = () => {
    return(
        <div className="container">
            <div className="py-4">
                <h1>Please run the command npm run json-server to populate data in Admin page</h1>
                <h2>After executing the above command refresh the app and goto Admin page</h2>
            </div>
        </div>
    )
}

export default Home;